---
title: "Statistique descriptive"
author: "Antoine Géré"
#date: last-modified
lang: "fr"
format: 
  revealjs:
    footer: "ISTOM, Ecole Supérieure d'agro-développement International"
    logo: ./img/Logo_ISTOM_Couleurs_HD-rbg.png
    theme: solarized
    transition: slide
    css: assets/style.css
    header-includes: |
      <script src="assets/custom.js" type="application/javascript"></script>
knitr:
  opts_chunk:
    dev: svg
    dev.args: { bg: "transparent" }
editor: visual
---

# Introduction

***Objectif*** : décrire de façon synthétique et parlante des données observées pour mieux les analyser.

::: callout-tip
## La statistique descriptive

La ***statistique descriptive*** étudie certaines caractéristiques d’une population. Ces caractéristiques sont appelées caractères ou variables statistiques.
:::

-   La statistique descriptive à une dimension ou univariée
-   La statistique descriptive à deux dimensions ou bivariée

# La statistique descriptive à une dimension / univariée

## Vocabulaire

-   ***Population*** : ensemble d’éléments homogènes auxquels on s’intéresse.
-   ***Individus*** : éléments de la population
-   ***Echantillon*** : une partie de la population étudiée

::: {.callout-note}
Les observations obtenues sur une population ou sur un échantillon forment un ensemble de données sur lesquelles s'appliquent méthodes de la statistique descriptive. Le ***choix de l'échantillon*** est donc ***important*** !
:::

## 

- Pour chaque individu → Une ou plusieurs variables  

  - Variable ***qualitative*** (nominale / ordinale)

  - Variable ***quantitative*** (intrinsèquement numérique)
  
    - Variable discrète (dans $\mathbb{Z}$)
    - Variable continue (dans $\mathbb{R}$)
    
    
Un peu de ***R*** pour illustrer ces notions. 

![](img/R_RStudio_LogoS-rbg.png){.r-stretch}

## 

On utilise le jeu de données « iris » de R.

```{r}
#| echo: true
#| message: true
data(iris) # charger les données
```

Il contient 150 observations de 5 variables.

::: columns
::: {.column width="70%"}

-   `Sepal.Length` : longueur de la sépale
-   `Sepal.Width` : largeur de la sépale
-   `Petal.Length` : longueur du pétale
-   `Petal.Width` : largeur du pétale
-   `Species` : espèce de la fleur.
:::
::: {.column width="30%"}
![](img/img_data_iris_r.png)
:::
:::

##

La fonction `head()` permet d’avoir un aperçu des premières lignes du jeu de données : 

```{r}
#| echo: true
head(iris)
```

On a ainsi : 

- ***1*** variable ***qualitative*** nominale (`Species`)

- ***4*** variables ***quantitatives*** continues (`Sepal.Length`, `Sepal.Width`, `Petal.Length` et `Petal.Width`)

## Représentations graphiques

Représensation graphique d'une population selon une variable ***qualitative***, ici `Species`


```{r}
#| echo: true
#| message: true
#iris$Species
mytable <- table(iris$Sepal.Length)
#mytable
mylabels <- names(mytable)
#mylabels
#pie(mytable, labels = mylabels, main="Pie Chart of Species", radius=1.08)
pie(mytable, radius=1.08)
```


##

- Tableau des effectifs pour une variable (`Species`) dans une population donnée

```{r}
#| echo: true
#| message: true
table_Species<-table(iris$Species)
table_Species
```

- Tableau des fréquences pour une variable (`Species`) dans une population donnée

```{r}
#| echo: true
#| message: true
prop.table(table_Species)
```

##

- Diagramme en baton des effectifs

```{r}
#| echo: true
#| message: true
barplot(table_Species)
```

::: {.callout-important}
Même choses pour les variables ***quantitatives discrètes***.
:::


[]: # Représensation graphique d'une population selon une variable ***quantitative continue***, ici `Petal.Width`

[]: # Domaine de variation d'une telle variable : partagée en $k$ parties.

[]: # $$[x_{i-1},x_i[ \quad \to \quad \mbox{i-eme classe}$$

[]: #```{r}
[]: #table_Sepal.Length<-table(iris$Sepal.Length)
[]: #table_Sepal.Length
[]: #```

## Les indicateurs statistiques

Graphiques : indicateurs pour appréhender la base de donnée étudiée.

Pour aller plus loin :

- la valeur de la variable située au "centre" de la distribution → la ***tendance centrale*** 

- ou plus généralement → indicateur de postion

- variations des valeurs → dispersion

- forme de la distribution 

- aspects particuliers → valeurs extrèmes, ...

##

### Indicateur de tendance centrale et de position 

mesures de tendances centrales → "moyennes"

Serie statistique brutes : $x_1$, $x_2$, $x_3$, ..., $x_n$.

- La ***moyenne arithmétique***
$$\bar{x} = \frac{1}{n} \sum_{i=1}^n x_i$$
Exemple : votre moyenne du module de sciences fondamentales !

##

- La ***moyenne géométrique***

→ lorsque la croissance est géométrique ou exponentielle

$$G = \sqrt[n]{\prod_{i=1}^n x_i} \quad \Longrightarrow \quad \ln(G) = \frac{1}{n} \sum_{i=1}^n \ln(x_i)$$
Exemple : 

## Le mode

- variable quantitative discrète 
  - valeur la plus fréquemment observée 
  - sur diagramme en baton  → somment de la distribution

- variable quantitative continue
  - fréquent que l'on observe jamais 2 fois la même valeur 
  - besoin de définir les classes : $\mbox{i-ème classe } \quad \to \quad [x_i,x_{_i+1}[$
  - ensuite même logique que pour une variable quantitative discrète
  
## Exemple avec la variable : `Petal_Length`
  
```{r}
#| echo: true
table_Petal_Length <- table(iris$Petal.Length)
#table_Petal_Length
#pie(table_Petal_Length)
#(table_Petal_Length)
prop_table_Petal_Length <- prop.table(table_Petal_Length)
#prop_table_Petal_Length
barplot(prop_table_Petal_Length)
```


## La mediane

On considère une rue droite, avec 5 maisons $M_i$ repérées par des abscisses $x_i$, où $i=1,...,5$.

***Objectif*** : déposer une benne à ordure $B$ (d'absisse $c$) à l'endroit le plus judicieux possible !

- Milieu des valeurs exrêmes : 
$$c = \frac12 \left(x_{min}+x_{max}\right)$$
Peu fiable, dépendance trop forte des valeurs extrêmes

## La mediane 

- La mediane d'un point de vue intuitif

idée : minimiser la distance totale entre les $M_i$ et $B$

La distance totale est
$$D(x) = \sum_{i=1}^5 |c-x_i|$$
Pour quelle(s) valeur(s) de $x$ la quantité $D(x)$ est minimale ?

## La mediane - Détermination pratique

::: {.callout-important}
1. cc
2. cc
:::



## L'étendue


## L'écart moyen absolu


## L'écart type et la variance


## L'écart interquartiles





##

La fonction `summary()` permet de visualiser des statistiques de base pour chaque variable.

- ***Pour les variables quantitatives*** : 
  - minimum, maximum, moyenne, médiane, 1er quartile, 3ème quartile. 
- ***Pour les variables qualitatives*** : 
  - nombre d’observations par classe
  
```{r}
#| echo: true
#| message: true
summary(iris)
```

On a aussi la fonction `describe` du package `Hmisc`. 

```{r}
#| echo: true
library(Hmisc)
describe(iris)
```


Cette fonction permet d’obtenir pour chaque variable : 

- le nombre d’observations, 
- le nombre de valeurs manquantes, 
- la moyenne, 
- les quantiles à 5, 10, 25, 50, 75, 90, et 95%, 
- les 5 plus basses valeurs, 
- et les 5 plus hautes valeurs.

Pour les variables qualitatives, describe() donne :

- le nombre d’observations, 
- le nombre de valeurs manquantes, 
- et le nombre de niveaux pour la variable ; ainsi que le nombre d’observations pour chaque niveau de la variable (et la proportion).


Variables quantitatives

```{r}
#| echo: true
#boxplot 
boxplot(iris$Sepal.Length, col = c("yellow"),main = "Boxplot", ylab = "Quantiles")
```


# La statistique descriptive à deux dimensions

coming soon.
